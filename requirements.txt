# ============================================
# CORE BACKEND
# ============================================
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9
websockets==12.0
python-dotenv==1.0.1
pydantic==2.6.1
requests==2.31.0

# ============================================
# VECTOR DB & CACHE
# ============================================
qdrant-client==1.9.0
redis==5.0.1

# ============================================
# LLM (Groq)
# ============================================
groq==0.5.0
httpx==0.27.2

# ============================================
# LANGUAGE DETECTION (replaces fasttext)
# ============================================
langdetect==1.0.9

# ============================================
# EMBEDDINGS & TRANSLATION (Hugging Face stack)
# ============================================
sentence-transformers==2.2.2
transformers==4.38.2
torch==2.2.0
huggingface-hub==0.20.3

# ============================================
# PDF EXTRACTION & DATA PROCESSING
# ============================================
PyPDF2==3.0.1
numpy==1.26.4

# ============================================
# (OPTIONAL) Cross-Encoder Reranker – requires sentence-transformers (already included)
# ============================================
# No extra package needed – sentence-transformers provides CrossEncoder

# ============================================
# (OPTIONAL) If you ever want to re-enable fasttext – uncomment:
# fasttext==0.9.2
# But we now use langdetect, so it's not required.
# ============================================